export enum GridPositions {
  Left = "LEFT", Right = "RIGHT", Center = "CENTER",
}

export enum Floors {
  Top = "TOP", Center = "CENTER", Bottom = "BOTTOM",
}

export enum Biomes {
  SPACE_COLONY = "SPACE_COLONY"
}
